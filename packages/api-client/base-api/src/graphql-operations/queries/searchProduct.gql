#import "../fragments/product.gql"

query SearchProduct($offset: Int!, $limit: Int!) {
  search(options: {currency: GBP, shippingDestination: GB, offset: $offset, limit: $limit, sort: NAME_ALPHABETICALLY, facets: []}, query: "") {
    hasMore
    products {
      ...Product
    }
  }
}
