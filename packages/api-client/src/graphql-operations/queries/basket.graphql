#import "../fragments/basketContents.graphql"

query Basket($id: ID, $currency:Currency! = GBP, $shippingDestination:Country! = GB) {
  basket(
    id: $id,
    settings: {
      currency: $currency
      shippingDestination: $shippingDestination
    }
  ) {
    ...BasketContents
  }
}
