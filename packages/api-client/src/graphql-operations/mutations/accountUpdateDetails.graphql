mutation UpdateAccountSettings($fieldList: [SettingsFieldInput!]!) {
  updateAccountSettings(input: $fieldList) {
    error
    fieldErrors {
      fieldName
      validators
      requiredButNotProvided
      invalidOption
    }
  }
}
